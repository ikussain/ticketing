<html>
  <body>

    <div class="nav">
      <ul>
        Ticketing
        <li><a href=<%=projects_path %>>Projects</a></li>
        <li>Tickets</li>
        <li>Tags</li>
      </ul>
    </div>

    <div class="header">
      <h1> Projects </h1>
    </div>

    <div class="list">
      <table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Open Tickets</th>
            <th>Total Tickets</th>
          </tr>
        </thead>

        <% @projects.each do |project| %>
          <td>
            <a href="<%= project_path(project.id) %>%"><h3><%= project.name %></h3></a>
            <p><%= project.description %><p>
          </td>
          <td><%= project.tickets.size %></td>
          <td><%= project.tickets.size %></td>
        <% end%>
      </table>
    </div>
  </body>
</html>